# New Room Template

## Metadata

```json
{
  "id": "new_room_id",
  "title": "New Room Title",
  "math_trick": "dominant math trick",
  "wrong_physics": "what physical law lies here",
  "shader_room_index": 12,
  "mood": "the emotional/liminal character",
  "palette": "color story",
  "tags": ["tag", "tag"]
}
```

## GLSL branch

```glsl
vec2 roomNewRoomTitle(vec3 p){
    // transform domain
    // define architectural SDFs
    // combine distances
    return res(d, materialId);
}
```

## World doc

Copy a file from `worlds/` and replace the identity sections.
